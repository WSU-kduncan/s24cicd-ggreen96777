#!/bin/bash

sudo apt update

# Installing packages that the images may need such as nginx, git, and python3 for future coding and development.
sudo apt install -y nginx python3 git

# More configurations or Installments could be implemented here, but we can start with these for now.
